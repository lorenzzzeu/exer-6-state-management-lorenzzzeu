class Item { // Define a class named Item
  String name; // Declare a string variable named name
  double price; // Declare a double variable named price
  int quantity; // Declare an integer variable named quantity
  
  Item(this.name, this.price, this.quantity);  // Constructor for the Item class with parameters name, price, and quantity
}